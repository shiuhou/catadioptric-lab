# 变更记录

## v7.0.0 · 2026-09-15 · 本地更新包

基于本次会话提供的 v6 `index.html`，不以未知的远程后续修改为基础。输入文件 SHA-256：`a9e94dce23728d53bc88e7e8cd72c52d6dceaa44075146a38e4dd6f119ae32f0`。

### 修复

- 焦距范围、多值、公差、等效焦距和单位不明的文字不再自动提取成一个确定值。
- 多采集分辨率提供未预选的候选列表；重复冲突字段在后续重复行中也不会重新被填上。
- 对比表保留空列，缺值不借用邻列或旧相机参数。
- 统一校验输入类型、整数像素数、软件计算范围和派生结果；非法导入原子拒绝，不部分修改设计。
- 本地保存失败不影响计算；非法临时参数不覆盖最后有效记录；清除后立即关闭不会重新生成已清除记录。

### 界面与使用

- 保留六页左侧导航、顶部实时图示及首页参数分组，不改成单参数模式。
- 主要 SVG 标签按最终屏幕尺寸放大；参数解释和触控控件增强可读性。小屏导航使用两列。
- 「对应公式」在图下原位展开，随当前参数实时更新；完整公式页与来源继续保留。
- 明确显示「固定视场 → 反算尺寸」/「固定尺寸 → 计算视场」和像素安全余量。
- 保留单次拖动的撤销 / 重做语义；显示比例、对照开关和完整重置也纳入撤销。页面导航本身不记一步。
- 宽屏图示工具栏增加固定比较基线入口。
- 新增最后有效设计的本地恢复提示；默认不持久化相机原文，保存设置在已有的「方案与导出」中公开可见。
- 规格核对区标记原文提取、手动确认及采集模式选择状态，保留源文和歧义记录。

### 工程与发布

- 沿既有模块边界拆出 `src/`；无需框架，构建继续产生一个无外部运行依赖的 `index.html`。
- 加入可重跑的数值、解析、回归及浏览器测试；保留修复前失败日志。
- README 改为用户入口，部署步骤移到 DEPLOYMENT.md；补模型、验证说明和版本号。
- 新增 meta description、canonical、Open Graph 分享信息和内嵌 favicon。未验证公网分享平台缓存刷新效果。

### 本版没有做

- 未更改正常参数域内的镜面 / 投影基本公式。
- 未添加账号、后台、遥测、云同步或参数分享链接。
- 未替项目持有人选择许可证，未提交或部署到 GitHub。
- 未验证实物对焦、标定、制造和飞行性能。
- 本次浏览器环境阻止 file:/localhost 导航；恢复逻辑使用显式 Storage API 测试替身验证，并包含存储拒绝和配额失败测试。原生磁盘持久性、真实 Windows / Safari / 手机浏览器需在目标环境复核。
